import '@payloadcms/next/css'
import type React from 'react'

// Minimal layout for the Payload admin panel — no site Navbar or Footer.
export default function PayloadLayout({ children }: { children: React.ReactNode }) {
  return children
}
